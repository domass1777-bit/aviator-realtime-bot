# Aviator Realtime Bot

Bot educacional para capturar multiplicadores do Aviator via WebSocket, analisar histórico e gerar sinais.

## Como usar
1. Capturar a URL do WebSocket do Aviator no navegador (F12 → Network → WS)
2. Colocar a URL em `main.py` na variável `ws_url`
3. Instalar dependências:
